/**
 * static @MyStatic: Static script for static experiments.
 * @Author: Vlad Gordienko.
 */
static script MyStatic
{
public:
    static fn Spam( integer n ) shell
    {
        integer i = 0;
        for( i=0; i<n; i++ )
            log( "[Static] Azaza from static %d", i );
    }

    static integer StaticInt;
    static bool    StaticBool;
 

 
}
